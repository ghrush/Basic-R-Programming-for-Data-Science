#CREATING AND USING VECTORS

#Create a vector of integers.
c(43, 42, 12, 8, 5)

#Save vector in a storage area.
myFamilyAges <- c(43, 42, 12, 8, 5)

#Add of the number in myFamilyAges.
sum(myFamilyAges)

#Display the values stored in myFamilyAges.
myFamilyAges

#Verify that R is case-sensative.
myFamilyages #Produces an error


#Display the range (min and max) of values in the vector myFamilyAges.
range(myFamilyAges)
