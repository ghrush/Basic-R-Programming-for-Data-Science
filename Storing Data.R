#Store text data in a named location.
myText <- "this is a piece of text"

#CREATING A DATA SET IN R

#Create a 'vector' -- a list where all of the items are of the same mode (type).
c(43, 42, 12, 8, 5)

#Store the vector in a named location.
myFamilyAges <- c(43, 42, 12, 8, 5)
